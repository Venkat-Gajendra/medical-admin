// Chart.js - v3.7.1
// http://www.chartjs.org
// ------------------

const helpers = {};

// ... (other helper functions)

const Chart = function (context, config) {
  this.config = config;
  this.ctx = context;
  this.chart = new this.constructor.defaults();

  // ... (other initializations)

  this.render();
};

Chart.defaults = {
  // ... (default configuration options)
};

Chart.extend = (SuperChart) => {
  const SubChart = function (context, config) {
    SuperChart.call(this, context, config);
  };

  SubChart.prototype = Object.create(SuperChart.prototype);
  SubChart.prototype.constructor = SubChart;

  // ... (other prototype methods)

  return SubChart;
};

// ... (other chart types like Bar, Line, etc.)

// Export Chart as a CommonJS module
if (typeof module !== 'undefined' && module.exports) {
  module.exports = Chart;
} else {
  // Export Chart as an AMD module
  if (typeof define === 'function' && define.amd) {
    define('chart', [], function () {
      return Chart;
    });
  } else {
    // Assign Chart to the global object (window in a browser)
    window.Chart = Chart;
  }
}
